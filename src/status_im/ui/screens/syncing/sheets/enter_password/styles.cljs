(ns status-im.ui.screens.syncing.sheets.enter-password.styles
  (:require [clojure.string :as string]
            [quo.design-system.colors :as colors]))

(def header-text
  {:font-size 27
   :font-weight :600
   :margin 20})

(def body-container
  {:margin 20})
