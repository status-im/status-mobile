(ns quo2.components.browser.dapp-item.style)

(def size 64)

(def logo
  {:height size
   :width  size})

(def gap
  {:height 8})

(def text
  {:max-width  size
   :text-align :center})

(def root-container
  {:padding     8
   :align-items :center})
