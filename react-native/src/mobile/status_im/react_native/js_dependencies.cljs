(ns status-im.react-native.js-dependencies
  (:require-macros [status-im.utils.js-require :as require]))

(def config                 (require/js-require "react-native-config"))
(def fs                     (require/js-require "react-native-fs"))
(def http-bridge            (require/js-require "react-native-http-bridge"))
(def keychain               (require/js-require "react-native-keychain"))
(def qr-code                (require/js-require "react-native-qrcode"))
(def react-native           (js/require "react-native"))
(def status-keycard         (require/js-require "react-native-status-keycard"))
(def realm                  (js/require "realm"))
(def webview-bridge         (require/js-require "react-native-webview-bridge"))
(def webview                (require/js-require "react-native-webview"))

(def react-native-securerandom (require/js-require react-native-securerandom))
(defn secure-random []         (.-generateSecureRandom (react-native-securerandom)))

(def EventEmmiter           (require/js-require "react-native/Libraries/vendor/emitter/EventEmitter"))
(defn fetch []                  (.-default (js/require "react-native-fetch-polyfill")))
(def i18n                       (js/require "i18n-js"))
(def react-native-languages     (.-default (js/require "react-native-languages")))
(def camera                 (require/js-require "react-native-camera"))
(def dialogs                (require/js-require "react-native-dialogs"))
(def dismiss-keyboard       (require/js-require "dismissKeyboard"))
(def image-crop-picker      (require/js-require "react-native-image-crop-picker"))
(def image-resizer          (require/js-require "react-native-image-resizer"))
(def svg                        (js/require "react-native-svg"))
(def react-native-firebase      (js/require "react-native-firebase"))
(def snoopy                 (require/js-require "rn-snoopy"))
(def snoopy-filter          (require/js-require "rn-snoopy/stream/filter"))
(def snoopy-bars            (require/js-require "rn-snoopy/stream/bars"))
(def snoopy-buffer          (require/js-require "rn-snoopy/stream/buffer"))
(defn background-timer []       (.-default (js/require "react-native-background-timer")))
(def react-navigation           (js/require "react-navigation"))
(defn react-native-mail []      (.-default (js/require "react-native-mail")))
(defn desktop-linking []        #js {:addEventListener (fn [])})
(defn desktop-menu []           #js {:addEventListener (fn [])})
(defn desktop-config []         #js {:addEventListener (fn [])})
(defn desktop-shortcuts []      #js {:addEventListener (fn [])})
